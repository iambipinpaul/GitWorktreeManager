<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid MinWidth="450" Background="{DynamicResource {x:Static styles:VsBrushes.ToolWindowBackgroundKey}}">
        <Grid.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.CheckBoxStyleKey}}" />
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Warning Icon/Text -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="⚠ Force Remove Worktree"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="#F48771"
                       Margin="0,0,0,12" />

            <TextBlock TextWrapping="Wrap" Margin="0,0,0,8">
                <Run Text="You are about to force remove the worktree " />
                <Run Text="{Binding WorktreeName, Mode=OneWay}" FontWeight="Bold" />
                <Run Text="." />
            </TextBlock>

            <TextBlock TextWrapping="Wrap" Margin="0,0,0,4">
                <Run Text="Path: " FontWeight="Bold" />
                <Run Text="{Binding WorktreePath}" />
            </TextBlock>
        </StackPanel>

        <!-- Checkbox Confirmation -->
        <CheckBox Grid.Row="1"
                  Content="{Binding ConfirmationText}"
                  IsChecked="{Binding HasConfirmedRisk, Mode=TwoWay}"
                  Margin="0,8,0,16"
                  Foreground="#F48771" />

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Force Remove"
                    Command="{Binding OkCommand}"
                    IsEnabled="{Binding IsValid}"
                    Padding="20,8" Margin="0,0,8,0" MinWidth="90" />
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Padding="20,8" MinWidth="90" />
        </StackPanel>
    </Grid>
</DataTemplate>
